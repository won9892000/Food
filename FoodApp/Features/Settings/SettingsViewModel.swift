import Foundation

class SettingsViewModel: ObservableObject {
    // Currently settings data comes from UserProfile via EnvironmentObject
    // This ViewModel is reserved for future settings-specific logic
}
